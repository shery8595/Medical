The testnet drip service now lives in **`../arb-sepolia-faucet/`** as a standalone package for its own GitHub repo.

1. Use **`arb-sepolia-faucet`** for source, `npm install`, and deploy.
2. Delete this **`faucet-server`** folder when nothing has it open (e.g. close editors using `node_modules` here), then remove the directory manually.
